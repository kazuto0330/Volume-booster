# Volume Booster (音量ブースター)

Webサイトの動画や音声の音量を、最大600%まで増幅できるChrome拡張機能です。
音が小さすぎる動画を視聴する際や、特定のサイトだけ音量を大きくしたい場合に便利です。

## 主な機能

*   **音量ブースト**: 0% ～ 600% の範囲で音量を調整可能。
*   **個別設定の保存 (Site-wide)**: ドメインや特定のURLパスごとに音量設定を保存できます。最長一致で最適な設定が自動適用されます。
*   **YouTube/Twitch詳細設定**: 
    *   YouTubeチャンネル（投稿者）ごとの設定や、ライブ配信時の自動減衰設定。
    *   YouTubeを開いた際のプレーヤー音量自動設定。
    *   動画プレーヤー上でのマウスホイールによる音量操作（オーバーレイ表示付き）。
*   **一時的な調整 (Current Tab)**: 現在開いているタブの音量だけを一時的に変更できます。この設定は保存されません。
*   **適用元の可視化**: 現在どの設定（サイト全体、アカウント別、一時設定など）が適用されているかを「適用中」タグで分かりやすく表示します。
*   **ダークモード対応**: OSの設定や好みに合わせて、ライトモードとダークモードを切り替えられます。
*   **多言語対応**: 日本語と英語に対応しています。
*   **Web Audio API**: ChromeのWeb Audio APIを使用して、音質の劣化を抑えつつ音量を増幅します。

## インストール方法 (開発者モード)

この拡張機能はまだChromeウェブストアに公開されていないため、以下の手順でインストールしてください。

1.  このリポジトリをダウンロード（またはクローン）して、適当なフォルダに解凍します。
2.  Google Chromeを開き、アドレスバーに `chrome://extensions/` と入力してアクセスします。
3.  画面右上の **「デベロッパーモード」** スイッチをオンにします。
4.  左上に表示される **「パッケージ化されていない拡張機能を読み込む」** ボタンをクリックします。
5.  手順1で解凍したフォルダ（`manifest.json` が含まれているフォルダ）を選択します。
6.  拡張機能が一覧に追加されればインストール完了です。

## 使い方

### ポップアップ画面
ブラウザ右上の拡張機能アイコンをクリックすると、設定ポップアップが開きます。

*   **Site-wide (saved)**:
    *   現在閲覧中のサイト（ドメイン）に対する音量設定です。
*   **Account (saved)**:
    *   YouTubeなどで利用可能な、投稿者（チャンネル）別設定です。表示されている場合のみ設定可能です。
*   **Current Tab (temporary)**:
    *   現在開いているタブのみに適用される一時的な設定です。タブを閉じるとリセットされます。
*   **適用中 (ACTIVE)**:
    *   現在どの設定値が実際の音量として適用されているかを示します。
*   **アイコン機能**:
    *   🗑️ (ゴミ箱): 設定のリセットメニューを開きます。現在適用中の設定を個別に削除したり、全設定をリセットできます。
    *   ☀️/🌙: テーマ（ライト/ダーク）の切り替え。
    *   ⚙️ (歯車): オプションページを開き、保存された設定を一覧管理できます。

### オプションページ
登録済みの設定を一覧で管理できます。

*   **一覧表示**: ドメイン別、アカウント別の設定が一覧表示されます。
*   **編集**: リストのドメイン名や数値をダブルクリックするか、編集ボタンを押すと内容を変更できます。
*   **追加**: ドメイン名とブースト率を入力して追加できます。
*   **マウスホイール**: スライダー上でマウスホイールを操作することでも数値を変更できます。

## 技術的な詳細

*   **Manifest V3**: 最新のChrome拡張機能仕様（Manifest V3）に準拠しています。
*   **Web Audio API**: `AudioContext`, `MediaElementSource`, `GainNode` を使用してオーディオ信号を処理しています。
*   **MutationObserver**: 動的に追加された動画や音声要素（YouTubeの自動再生など）も検知してブーストを適用します。
*   **Storage API**: `chrome.storage.sync` を使用して、デバイス間で設定を同期します。

## 権限について

*   `storage`: 設定（音量、テーマ、言語）を保存するために使用します。
*   `scripting`: Webページに音量制御スクリプトを注入するために使用します。
*   `activeTab` / `<all_urls>`: どのWebサイトでも音量を制御できるようにするために必要です。ユーザーの閲覧履歴や個人情報を収集することはありません。

## 変更履歴
### v1.2 (2026-02-25)
*   **マウスホイール音量制御**:
    *   YouTubeおよびTwitchのプレーヤー上でマウスホイールを使用して音量を調整できる機能を追加。
    *   音量変更時にプレーヤー上に現在の音量をオーバーレイ表示。
*   **YouTube機能の更なる拡張**:
    *   YouTubeを開いた際にプレーヤーの音量を指定値に自動設定する機能を追加。
    *   特定のチャンネル（アカウント）別設定の精度向上。
*   **マッチング精度の向上**:
    *   ドメインだけでなく、URLパスに基づいた最長一致による設定適用に対応。
*   **UI/UXのブラッシュアップ**:
    *   音量調整範囲を 0% ～ 600% に拡大。
    *   各設定項目の意図を説明するツールチップをポップアップに追加。

### v1.1 (2026-01-05)
*   **YouTube機能拡張**:
    *   チャンネル（アカウント）ごとの音量設定機能を追加。
    *   YouTube Live（生放送）視聴時専用の音量設定機能を追加。
*   **UI/UX改善**:
    *   ポップアップに「適用中 (ACTIVE)」タグを追加し、現在の音量設定の適用元を明示。
    *   設定リセット機能を強化（個別削除、全削除メニュー）。
    *   オプションページのデザインを刷新。ダブルクリックでの編集やマウスホイールでのスライダー操作に対応。
    *   ドメイン入力時の `https://www.` 自動削除機能。

### v1.0 (2025-12-20)
*   **初回リリース**: 基本的な音量ブースト機能の実装。
*   **機能追加**:
    *   Web Audio APIによる10%～600%の音量調整。
    *   ドメインごとの設定保存機能。
    *   タブごとの一時的な音量変更機能。
    *   ライト/ダークモード切替機能。
    *   日本語/英語の多言語対応。